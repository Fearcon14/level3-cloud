# Optional: API backend for local dev (Vite proxy target).
# - Leave unset to use http://localhost:8080 (run API locally).
# - Set to your cluster LoadBalancer URL (e.g. http://<LB_IP>) when testing against cluster API.
# VITE_API_PROXY_TARGET=http://192.168.1.100
