apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: paas-tls
  namespace: default
spec:
  secretName: paas-tls
  dnsNames:
    - kevin-sinn.runs.onstackit.cloud
  issuerRef:
    kind: ClusterIssuer
    name: letsencrypt-prod

